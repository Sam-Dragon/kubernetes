# Resource Quota
- It allows us to configure
  1. Default memory and cpu
  2. Minimum memory and cpu
  3. Maximum memory and cpu

> resource-quota.yaml
vi resource-quota.yaml
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: resource-quota
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 25Mi
    limits.cpu: "2"
    limits.memory: 256Mi
    type: Container
---
> copy and save
kubectl create -f resource-quota.yaml -n ckad (OR) kubectl apply -f resource-quota.yaml -n ckad

# Pod
kubectl run nginx-quota --image=nginx -n ckad 
